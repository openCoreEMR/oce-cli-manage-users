name: PHPUnit

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  phpunit:
    uses: openCoreEMR/github-workflows-public/.github/workflows/php-tests.yml@1.0.0
    with:
      php-versions: '["8.2"]'
